---
name: "video-to-mindmap"
description: "通用多模态视频转思维导图工作流。面向任意知识领域长视频（理工科技、工程实操、艺术设计、人文社科等），通过双轨解耦流水线（时序语义流 + 场景突变高熵视觉流）进行原子化提炼与重难点辅助记忆，智能捕获纯文字难以表达的高信息熵视觉界面（架构拓扑、状态机、流程闭环、坐标模型等）并进行自适应红框与高亮批注，生成无缝兼容 XMind 与交互式 HTML 预览的多模态思维导图。"
---

# 视频转思维导图（Video to Mindmap）通用多模态工作流

本技能提供了一套从**任意领域视频链接**到**高密度多模态知识导图**的通用工程化闭环。其核心是借鉴高水准手工思维导图的**“知识原子化提炼法”**与**“多模态视觉锚定法”**，彻底消除空洞过场废话，在保证 100% 逻辑深度与关键图表精准呈现的同时，实现极低的冗余消耗与秒级响应。

---

## 一、 核心设计哲学（三大通用认知支柱）

### 1. 知识原子化与去废话协议（Atomic Knowledge & Zero Fluff）
- **彻底去除叙述性水分**：严禁出现“本节主要介绍了”、“众所周知”、“在当今业界十分流行”等任何过渡性套话，每个节点必须是高信息密度的**知识原子**。
- **概念追本溯源（术语/英文原名/词根）**：关键术语、工具、算法、概念必须注明官方英文原名或缩写，辅助还原技术脉络与底层本义。
- **通用三层递进展开语法**：
  $$\text{核心概念/模块 (英文全称/缩写): 核心本质与定义} \longrightarrow \text{底层机制 / 关键要素 / 运行规则} \longrightarrow \text{参数指标 / 边界条件 / 适用场景}$$
- **节点短语化与零终结符**：统一采用 `维度/标签: 说明内容` 呈现，末梢枝叶结尾**严禁加句号**（`。` 或 `.`）。

### 2. 重难点极简辅助记忆（Mnemonic Anchoring）
- **高对比矩阵微表格**：当视频中出现多方案、多版本、多策略横向对比时，在节点下内嵌 2×2 或 3×3 Markdown 微表格，实现多维信息的瞬间聚合。
- **参数与定量关系精确化**：所有涉及的数值、阈值、比例、时间复杂度与定量关系必须明确给出，公式一律使用规范 Unicode 字符（如 `x²`、`log₁₀(P)`、`O(n²)`、`α`、`β`、`→`），严禁未转义的 LaTeX 标记。

### 3. 多模态视觉锚定与自适应场景批注（Adaptive Visual Anchoring）
视频相比于纯文字的核心优势在于**高信息熵的视觉信息呈现**。但视觉呈现必须遵循**信息对称与认知公平原则**，严禁机械地为所有图片滥套红框：
- **全局平衡架构**：系统分层、网络拓扑、闭环流转，必须保持原图客观中立，绝不人工偏袒单一部件；
- **微观局部焦点**：仅在密集工程面板中为了辅助读者 0.1 秒定位微观参数时，采用微透双层红框与聚光灯暗角；
- **对称方案对峙**：对立实体交互（Client vs Server）采用平权双色或对称框线；
- **动态演进过程**：多瞬时状态采用水平时序胶卷拼接呈现。

---

## 二、 通用视觉-语义判据与图文降级铁律（Domain-Agnostic Visual-Semantic Criteria）

在评估多模态知识视频时，严禁陷入“有讲必有图、图随声走”的**音频先验强行绑定幻觉（Audio-First Hallucination）**。必须确立：**音频语音流是连续线性的，而具备不可约减认知价值的高熵结构化图形载体是高度离散且极度稀疏的**（30 分钟理论视频中通常仅有 1~3 处真实图表，甚至全片为 0）。

在筛选画面与制定配图决策时，严格执行以下**四大通用视觉-语义判据**：

### 1. 语义载体的直接对应性检验（Direct Semantic Carrier vs. Metaphorical Wallpaper）
* **形式化映射准入**：画面中的几何图形、坐标轴、数据走势、网络拓扑或工程控制台本身，即为该概念/模型的**形式化逻辑载体（Formal Representation）**。离开此画面，读者的认知成本显著上升；
* **隐喻性衬底强制熔断**：若讲师口述的是高维抽象理论模型（如宏观周期、供需关系、算力杠杆、系统架构），而画面展示的是具象物质隐喻或影视化衬底（如泛用空镜、办公桌键盘、货币钞票特写、无实质逻辑的讲师特写等），**100% 判定为语义脱靶，坚决不作为知识图表采纳，严禁截图！**

### 2. 信息的不可约减空间拓扑性（Irreducible Spatial/Topological Value）
* **媒介优势权比较法则**：该画面的信息是否可用 Markdown 树状节点、微表格（2×2/3×3）或 Unicode 流程链（A → B → C）无损且零延迟复刻？
  * **文字/表格降维法则**：单向线性流（A→B→C）、二维属性对照（参数对比表）、纯文本提纲等低维信息，**纯文本形态在排版清晰度、全文检索性、免加载损耗上全面碾压截图**，必须强制降级为纯文本/微表格；
  * **高维拓扑准入法则**：仅当画面呈现**非线性网状回路、跨维度坐标分布、复杂量化数据走势、多层嵌套拓扑或高密工程交互面板**等纯文字难以低成本还原的空间几何拓扑时，截图才具备存在的合法性。

### 3. 批注图元的实体对齐性（Entity-Level Annotation Alignment）
* **局部基元对齐**：所有红框、高亮焦点、序号球（①②③）标注的像素落点，必须是客观存在的**独立语义图形基元（Semantic Visual Primitive）**（如状态机的一个闭合状态节点、系统框图的独立模块、图表坐标轴的突变拐点）；
* **严禁虚空赋义**：绝对禁止在一块连续自然实景纹理、非对应物体或无独立闭合边界的衬底画面上，凭空圈出一块区域并强行冠以抽象理论标签。

### 4. 安全视窗与去字幕协议（Subtitle-Safe Cropping Protocol）
* **规避外挂跑马灯字幕**：科技与商业视频底部通常带有自动生成或人工外挂的字幕遮挡。截取高熵图表时，必须精准识别图表主体边界或裁剪掉底部字幕视窗（Subtitle Zone），确保导图内嵌图表的客观中立与视觉纯净度。

---

### 画面决策矩阵与降级行动表

| 画面类型与本质特征 | 视觉判定与通用表征 | 决策行动与处理策略 | 核心认知理由与替代标准 |
| :--- | :--- | :--- | :--- |
| **低熵 A 类：具象隐喻与氛围衬底** | 讲师特写、无逻辑空镜、情绪垫片、泛用象征素材等 | **绝对剔除，严禁截图** | 语义脱靶。声音讲抽象理论，画面仅为装饰性衬底，配图会引入严重认知噪声。 |
| **低熵 B 类：大字提纲与线性列表** | 纯文字 PPT、项目符号清单、单向 3~4 步线性卡片等 | **绝对剔除，强制降级** | **文字表达完胜截图**！直接降维转化为 3 级 Markdown 树状节点或 Unicode 流程链（`A → B → C`），支持全文检索且零加载损耗。 |
| **低熵 C 类：二维参数与多方案对比** | 讲师板书或课件罗列的方案对比表、端口对照清单等 | **绝对剔除，降级为微表格** | 降维转化为 2×3 或 3×3 Markdown 微表格，阅读排版与结构清晰度远优于截图。 |
| **高熵 D 类：全局平衡拓扑网络** | 5+ 节点的系统分层架构、状态转移网、生化代谢回路等 | **准入，采用【模式 0：零标注纯景】** | 纯文字难以直观还原空间多向关联。**严禁画框**，保持各节点绝对客观平权。 |
| **高熵 E 类：密集工程界面与微观参数** | 64 轨 DAW 走带、Wireshark 报文堆栈、复杂 IDE 调试面板等 | **准入，采用【模式 1：微观焦点引导】** | 元素极度繁杂，读者无法瞬间找到讲解焦点，需双层细框或聚光灯暗角引导。 |
| **高熵 F 类：双端对峙与时序演进** | Client-Server 报文交互、动态操作前后状态对比等 | **准入，采用【模式 2 或 模式 3】** | 采用双色对称框（模式 2）或多帧水平胶卷拼接条（模式 3）。 |

> [!NOTE]
> **全篇零配图崇高性声明：**
> 若全视频均为口述解说、隐喻垫片或文字提纲课件，**产出 100% 纯文本+微表格的“零配图纯净思维导图”是完全合规且极受推崇的最高信息密度形态**！导图是高密度知识外脑，绝不为凑配图指标而制造视觉垃圾。

---

## 三、 解耦式工程架构与四级自适应降级阶梯（Degradation Ladder）

本技能采用**时序语义轨**与**自适应事件视觉轨**完全解耦的架构，根据宿主环境支持的 Payload 特征与算力条件自适应适配：

```text
                     [ 任务触发：视频 URL 输入 ]
                                 │
                 【步骤 -1：宿主 Agent 能力握手自检】
                                 │
       ┌─────────────────────────┼─────────────────────────┐
       ▼                         ▼                         ▼
【梯级 1：长视频原生流】    【梯级 2：图文混合流(默认)】   【梯级 3：纯文本极速流】
(支持视频句柄直传API)      (支持图片查看与长文本)        (纯文本推理模型/禁用视觉)
       │                         │                         │
 检查视频时长:             视频下载 + 突变抽帧:           直接纯文本轻量处理:
 • ≤ 45~60min: 直传        • 提取 transcript.txt         • 挂载 --skip-download
 • > 60min: 自动切片       • 抽 15~25 张候选高熵图        • 跳过视频/抽帧耗时
 (按45min分段切割)         • 锁死候选清单审阅批注         • 10秒极速产出纯文本导图
       │                         │                         │
       └─────────────────────────┼─────────────────────────┘
                                 ▼
                    【汇总生成统一思维导图】
```

### 四级自适应降级阶梯（Degradation Ladder）
1. **梯级 1（长视频原生流 / Native Media Stream）**：
   - **适用环境**：宿主环境原生支持音视频文件句柄直接传递与服务端状态化缓存（如 Gemini 1.5 Pro / 2.0 原生媒体 API）。
   - **时长策略**：$\le 45\sim 60$ 分钟直传；$> 60$ 分钟自动调用切片引擎（`--slice-minutes 45`）分段切割与递归提炼。
2. **梯级 2（时序图文混排自适应流 / Interleaved Multimodal - 默认主力推荐）**：
   - **适用环境**：宿主环境支持多图查看与大文本混合输入（如 Claude 3.5 Sonnet、GPT-4o、Gemini Flash）。
   - **组织形式**：严禁直接投喂几 GB 视频原片！运行 `process_video.py` 提取清洗文稿，配合 `detect_keyframes.py` 提取 15~25 张候选关键帧，在时序轴上交错编排。首字响应（TTFT）提升 5~10 倍。
3. **梯级 3（纯文本语义流极速通道 / Pure-Text Fast-Track）**：
   - **适用环境**：宿主为纯文本模型（如 DeepSeek-R1 / V3、Qwen-Coder、本地开源纯文本模型），或用户显式要求极速生成。
   - **组织形式**：`process_video.py` 强制挂载 `--skip-download`，仅下载清洗字幕，完全跳过视频与抽帧。执行【图文表格降维铁律】产出 100% 纯文本+微表格导图。**10~15 秒极速交付，100% 免疫画面与坐标定位幻觉**，网络消耗降为几百 KB。
4. **梯级 4（无原生字幕/纯音频 ASR 兜底通道 / Audio-ASR Fallback）**：
   - **适用环境**：视频无外挂 CC/AI 字幕。自动以最快速度抓取/抽取纯音频轨（`_audio.m4a`），调用本地 Whisper 或云端 ASR 生成秒级时间戳文稿，无缝接回上述梯级。

---

## 四、 目录与产物归档规范

每个视频任务在 `./downloads/<清理后的视频标题>/`（或环境变量 `OUTPUT_BASE_DIR` 指定目录）下独立建立专属工作空间：

```text
./downloads/<视频标题>/
├── <视频标题>.mp4                   # 视频媒体原片（用于高保真截帧与多模态分析）
├── <视频标题>.info.json              # 视频元数据（作者、发布时间、章节、简介等）
├── <视频标题>_transcript.txt         # 提取并清洗的时间戳文稿
├── slices/                          # 长视频分段切片目录 (当启用 --slice-minutes 时生成)
│   ├── slices_manifest.json         # 分段索引清单 (记录各分段起止时间、文件路径)
│   ├── <视频标题>_part01.mp4         # 分段切片视频 (流拷贝无损分段)
│   └── <视频标题>_transcript_part01.txt # 对应分段清洗文稿
├── keyframes/                       # 自适应场景突变提取的候选关键帧集与索引清单
│   ├── keyframes_manifest.json      # 关键帧时间戳与文件索引清单
│   └── keyframe_01_00-15.png
├── images/                          # 最终入选导图的高熵自适应配图目录（如无需配图则为空）
│   ├── <知识点1_全景图>_pure.png     # 模式0：无标注客观纯景图 (最推荐)
│   └── <知识点2_聚焦图>_focus.png    # 模式1：微观焦点引导图
├── <视频标题>_mindmap.md            # XMind 兼容 & 多模态纯净 Markdown 思维导图
└── <视频标题>_mindmap.html          # 自包含交互式 SVG 思维导图网页
```

---

## 五、 端到端标准执行流水线（全自主闭环流）

> [!IMPORTANT]
> **无人值守连续执行协议与自愈例外：**
> 本技能通常为全自主端到端闭环，严禁在各步骤之间无故停顿交出控制权。
> **唯一合法交互例外**：当执行【步骤 0 鉴权预检】检测到该平台 Cookie 缺失或失效（抛出 `AUTH_REQUIRED` 信号）时，Agent **必须主动向调用人发起交互**，提供二维码扫码链接或凭据录入指引；在调用人扫码或提供 Cookie 后，流水线立即自愈并自动继续向下执行！

### 步骤 -1：宿主能力识别与降级协议握手（Host Capability Handshake）
在执行任何实际命令前，Agent 必须首先自检当前宿主环境的输入模态与算力条件，决定后续执行分支：
- [ ] **分支 A（纯文本推理模型 / 禁用视觉 / 极速需求）**：
  - 执行**极速纯文本通道**：在步骤 1 强制挂载 `--skip-download`；
  - **完全跳过步骤 2（抽帧）与步骤 4（批注）**；
  - 严格执行【图文表格降维铁律】，将讲师口述与要点转化为 Markdown 树状节点，对比信息转化为 2×2/3×3 微表格；
  - 直接进入步骤 5 生成纯文本导图并用 `cli.py render` 渲染交付。
- [ ] **分支 B（图文多模态模型 - 默认推荐主力）**：
  - 执行**双轨解耦流程**：正常执行步骤 1 下载与步骤 2 抽帧；
  - 在步骤 3、4 中审阅候选帧清单并进行决策。
- [ ] **分支 C（长视频分段切片处理 - 视频时长 > 60 分钟）**：
  - 在步骤 1 中主动追加 `--slice-minutes 45` 参数；
  - 针对 `slices/` 下各分段独立产出知识分支树，最后由总控汇总缝合为一个统一中心主题（`#`）的完整多模态导图。

### 步骤 0：多平台身份鉴权预检与自愈拦截（Auth Preflight & Self-Healing）
```bash
python scripts/auth_manager.py check "<视频URL>"
```
- **核心职责**：
  1. 自动识别 URL 平台（B站、YouTube、抖音等）并展开/净化短链（如 `b23.tv` 清洗跟踪参数并保留分P）；
  2. 探测 `F:\yt-dlp\cookies\<平台>_cookies.txt` 是否有效，彻底杜绝与日常运行中的 Edge/Chrome 发生数据库锁争抢；
  3. 若凭证有效，`process_video.py` 自动挂载 `--cookies` 启动；
  4. 若凭据缺失或失效（抛出 `AUTH_REQUIRED`），Agent 立即呼起人机交互：
     - **B站专属免密扫码**：运行 `python scripts/auth_manager.py login-qr bilibili`，生成二维码供用户手机扫码，官方 API 秒级捕获会话并持久化，后续数月自动轮换续杯；
     - **YouTube / 抖音 / 通用平台（小白保姆级获取与自愈指引）**：
       当上述平台因风控拦截需要 Cookie 时，**Agent 严禁仅向用户抛出抽象晦涩的“请提供 Cookie”，必须主动向操作者输出以下通俗易懂的 SOP 指引**，并附带简要技术原理解释（告知用户因 Windows 11 / Chromium 最新的 App-Bound Encryption 机制，系统级拦截了外部工具静默解密浏览器 Cookie，故需一次性授权交接）：
       * **SOP 路径 1（最推荐·免装插件 10 秒 F12 复制法）**：
         1. 操作者在日常浏览器（Edge/Chrome）中打开该平台并确保处于登录状态；
         2. 按键盘 `F12`（或右键网页空白处选择「检查」），切换到 **「网络 / Network」** 选项卡；
         3. 按 `F5` 刷新一次网页，在左侧请求清单最顶部点击第一个请求（通常为主域名如 `www.youtube.com` 或具体的 `watch?v=...`）；
         4. 在右侧 **「标头 / Headers」** 面板中向下滚动，找到 **「请求标头 / Request Headers」** 区域的 **`Cookie:`** 这一项；
         5. 右键该项选择 **「复制值 / Copy value」** 并直接粘贴发送给 Agent；
         6. Agent 接收后自动执行 `python scripts/auth_manager.py import-cookie <platform> --cookie-str "<raw_cookies>"` 完成解析与持久化归档，随后流水线自动自愈并继续往下执行！
       * **SOP 路径 2（一劳永逸·扩展导出文件法）**：
         1. 操作者在 Edge/Chrome 扩展商店安装轻量导出扩展（如 `Get cookies.txt LOCALLY` 或 `Cookie-Editor`）；
         2. 打开目标平台页面，点击扩展图标导出为 Netscape 格式的 `cookies.txt`；
         3. 将下载的文件重命名并归档至 `F:\yt-dlp\cookies\<平台>_cookies.txt`（例如 `youtube_cookies.txt`），或直接将文件路径告知 Agent；
         4. Agent 执行 `python scripts/auth_manager.py import-file <platform> --file "<路径>"` 自动完成归档并无缝继续流水线。

### 步骤 1：视频抓取、文稿清洗与自适应切片
```bash
# 标准图文模式 (默认)
python scripts/process_video.py "<视频URL>"

# 纯文本极速模式 (跳过视频下载，10秒产出，网络消耗降低99%)
python scripts/process_video.py "<视频URL>" --skip-download

# 长视频自动分段切片 (时长 > 60分钟推荐，按45分钟切片防溢出)
python scripts/process_video.py "<视频URL>" --slice-minutes 45
```
*(优先提取原生 CC 或 AI 字幕并清洗为秒级时间戳文稿；若无字幕，自动提取纯音频轨备用；若开启切片，自动在 `slices/` 生成分段视频与对应文稿清单)*

### 步骤 2：自适应高信息熵候选帧提取 (纯文本模式跳过)
```bash
python scripts/detect_keyframes.py "<视频路径>" --max-frames 20
```
*(基于场景突变率自适应提取 15~25 张候选静态关键帧，生成 `keyframes_manifest.json`；纯文本模式直接跳过本步骤)*

### 步骤 3：深度语义分析与四大视觉判据决策
1. 结合文稿结构化提炼知识原子，遇对比优先提炼为 Markdown 微表格；
2. 对照候选关键帧严格执行**【四大通用视觉-语义判据】**：
   - **语义载体对应性检验**：画面是否为当前抽象理论的形式化逻辑载体？若仅为具象隐喻衬底、主播特写或无实质逻辑的空镜 $\rightarrow$ **直接熔断，严禁截图！**
   - **空间拓扑不可约减性检验**：若画面为文字型 PPT、线性步骤或简单键值对比 $\rightarrow$ **坚决不插图，强制降级为 Markdown 树状节点或微表格**；
   - **实体基元对齐检验**：若确有纯文字无法还原的高维拓扑或密集工程面板，且批注落点具备独立闭合图形基元 $\rightarrow$ 判定知识形态并自适应选择模式 0、1、2 或 3；
3. **全篇零配图合规意识**：若视频各章节均未出现原生高价值图形载体，坚决输出 100% 纯文本+微表格的“零配图纯净思维导图”。

### 步骤 4：自适应多模态视觉处理与防幻觉批注（若入选高熵图）

> [!CAUTION]
> **防幻觉与防坐标漂移执行铁律（Anti-Hallucination Safety Protocol）：**
> 1. **锁死候选帧来源，严禁虚空盲截**：
>    严禁 Agent 凭空从文稿时间中猜测 `--timestamp` 截取视频原片（极易截出转场黑屏或讲师人脸）！
>    **强制标准流程**：必须优先审阅并直接复用 `keyframes/keyframes_manifest.json` 中标记为 `is_graphic_candidate: true` 的已有关键帧图片路径！
> 2. **坐标置信度降级铁律（Coordinate Fallback Rule）**：
>    面对密集工程界面，大模型估算的归一化坐标极易发生漂移错位。
>    **铁律：除非具备 100% 确定性可定位到独立闭环组件，否则一律优先采用【模式 0：全景零标注纯景（pure）】或【主体智能剪裁（--crop-focus）】，绝对禁止凭空捏造虚空坐标！**
>    全景纯景图不仅 100% 免疫坐标漂移，而且在认知上保持各组件平权中立，是业界最推崇的高保真知识呈现形态。
> 3. **遏制炫技偏置（Anti-Overfitting）**：
>    放大镜、步骤球、荧光笔等高级图元仅用于特定微观排错或复杂流程，绝不可为了“展示工具功能”而滥套图元。

根据选定模式调用增强版 `extract_frame.py`（**优先直接输入已提取的关键帧图片路径**）：
- **模式 0（全景零标注纯景 - 默认最推荐，100% 免幻觉）**：
  ```bash
  python scripts/extract_frame.py "downloads/<标题>/keyframes/keyframe_03_05-20.png" -o "downloads/<标题>/images/<名称>_pure.png" --mode pure
  ```
- **模式 1（微观局部焦点引导 - 仅在坐标绝对确信时使用）**：
  ```bash
  python scripts/extract_frame.py "downloads/<标题>/keyframes/keyframe_03_05-20.png" -o "downloads/<标题>/images/<名称>_focus.png" --mode focus --box "<x1,y1,x2,y2>" --label "<焦点标签>" [--spotlight]
  ```
- **模式 2（双向平权对比）**：
  ```bash
  python scripts/extract_frame.py "downloads/<标题>/keyframes/keyframe_03_05-20.png" -o "downloads/<标题>/images/<名称>_dual.png" --mode dual --boxes "<b1;b2>" --labels "<l1;l2>"
  ```
- **模式 3（时序胶卷拼接）**：
  ```bash
  python scripts/extract_frame.py "<视频路径>" --timestamps "<t1,t2>" -o "downloads/<标题>/images/<名称>_filmstrip.png" --mode filmstrip --labels "<步骤1;步骤2>"
  ```
- **高级图元组合（可独立或与基础模式正交叠加，优先传入现有关键帧）**：
  ```bash
  # 步骤序号球 (① ② ③) + 胶囊药丸说明
  python scripts/extract_frame.py "downloads/<标题>/keyframes/keyframe_01.png" -o "<输出>" --steps "x1,y1:步骤A;x2,y2:步骤B"
  
  # 局部画中画超分放大镜 (Loupe)
  python scripts/extract_frame.py "downloads/<标题>/keyframes/keyframe_01.png" -o "<输出>" --zoom "cx,cy,r" --zoom-scale 2.5
  
  # 真实光学荧光笔涂抹 (Highlight) + 定向流程箭头 (Arrow)
  python scripts/extract_frame.py "downloads/<标题>/keyframes/keyframe_01.png" -o "<输出>" --highlight "x1,y1,x2,y2" --arrow "x1,y1->x2,y2:接口流向"
  
  # 隐私凭据与私有 IP 局部脱敏 (Blur / Pixelate)
  python scripts/extract_frame.py "downloads/<标题>/keyframes/keyframe_01.png" -o "<输出>" --blur "x1,y1,x2,y2"
  ```
  *(注：所有坐标原生支持 `0.0~1.0` 归一化比例与绝对像素；直接传入关键帧图片不仅秒级完成，更能彻底杜绝重复调用 ffmpeg 截帧的音画漂移！)*

### 步骤 5：生成通用高密度多模态 Markdown 思维导图
保存为 `<视频标题>_mindmap.md`。

**标准模板规范：**
```markdown
# <视频核心主题（全篇唯一一级大标题，即导图中心主题）>

## 1. <一级分支 / 核心模块或核心阶段 1（主要主题，带数字编号）>
### <二级分支 / 核心概念、体系架构或方法分类 1.1>
#### <三级分支 / 底层机制、组成维度或实操步骤 1.1.1>
##### ![高信息熵界面图解与红框批注说明](./images/<对应截图>_pure.png)
##### <四级分支 / 知识原子：核心定义与本质（标注英文原名/缩写/词源），结尾严禁加句号>
##### <四级分支 / 运作机制：输入、处理逻辑与输出结果，结尾严禁加句号>
##### <四级分支 / 核心参数或定量关系：使用规范Unicode表达，结尾严禁加句号>
#### <三级分支 / 方案对比或评估指标 1.1.2>
| 对比维度 | 方案/对象 A | 方案/对象 B | 选型考量/适用边界 |
| :--- | :--- | :--- | :--- |
| **维度一** | 特征参数说明 | 特征参数说明 | 决策原则 |
| **维度二** | 特征参数说明 | 特征参数说明 | 决策原则 |
##### <四级分支 / 边界条件与避坑指南：说明内容，结尾严禁加句号>

## 2. <一级分支 / 核心模块或核心阶段 2（主要主题，带数字编号）>
### <二级分支 / 核心机制或流程推进 2.1>
#### <三级分支 / 关键链路与视觉流程 2.1.1>
##### ![关键流程图解与红框批注](./images/<对应截图>_focus.png)
##### <四级分支 / 第一步：前置条件与核心输入，结尾严禁加句号>
##### <四级分支 / 第二步：核心处理与变换机制，结尾严禁加句号>
##### <四级分支 / 第三步：交付产物与验收标准，结尾严禁加句号>

## 3. <一级分支 / 核心模块或核心阶段 3（依据内容完整穷尽后续章节）>
...
```

> [!CAUTION]
> **XMind 零报错与排版红线：**
> 1. 全篇必须且只能有 1 个 `#` 根标题，杜绝画布分裂；
> 2. 严禁插入综述长篇段落，严禁 `---` 分割线，严禁内嵌 ````mermaid```` 代码块；
> 3. 各级分支与末梢枝叶结尾**绝不加句号**；
> 4. 所有公式、定量符号必须使用 Unicode（严禁 LaTeX 标记）；
> 5. 图片严格采用相对路径 `![说明](./images/xxx.png)`，确保 XMind 导入时自动就地内嵌到节点气泡。

### 步骤 6：编译交互式 HTML 思维导图
```bash
python cli.py render "<生成的_mindmap.md绝对路径>" --embed-images
```
* 提示：使用 `--embed-images` 参数可将本地图片自动转为 Base64 内嵌，实现完全独立离线的便携单文件交付。

### 步骤 7：交付成果汇报
向用户汇报生成结果：
1. **项目专属文件夹超链接**（`file:///<导图目录绝对路径>/...`）；
2. **纯净 Markdown 导图预览**（直接在聊天框完整展示）；
3. **视觉截帧展示**（展示抽取的带红框批注关键图片）；
4. **多端使用指引**（直接在浏览器双击 `.html` 享受桌面级交互与全屏灯箱；或在网页右上角直接点击「导出原生 .xmind」免配置下载）。

---

## 六、 参考资料与工具清单

- **控制中枢与自检工具**：[cli.py](./cli.py)（支持 `python cli.py doctor` 环境体检自愈）
- 通用多模态视频理解与 Token 压缩规范：[multimodal_pipeline_2026.md](./references/multimodal_pipeline_2026.md)
- 多模态导图格式与 Unicode 符号大全：[mindmap_formats.md](./references/mindmap_formats.md)
- 标准输出样例：[sample_mindmap.md](./examples/sample_mindmap.md)
- 视频下载与文稿清洗脚本：[process_video.py](./scripts/process_video.py)
- 自适应高信息熵关键帧提取脚本：[detect_keyframes.py](./scripts/detect_keyframes.py)
- 像素级红框批注与裁切脚本：[extract_frame.py](./scripts/extract_frame.py)
- 交互式 HTML 导图生成脚本：[generate_html.py](./scripts/generate_html.py)

